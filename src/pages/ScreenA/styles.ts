import { StyleSheet } from 'react-native';
import { GOOSE_PINK } from '../../constants.styles';

const style = StyleSheet.create({
  safeArea: {
    backgroundColor: GOOSE_PINK,
  },
  topContainer: {
    backgroundColor: GOOSE_PINK,
    height: '45%',
  },
});

export default style;
